interface:
  display_name: "VIBE 1"
  short_description: "Repo-specific patterns and workflows for VIBE-1"
  default_prompt: "Use the VIBE-1 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true